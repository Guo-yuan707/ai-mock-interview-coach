"""golden 评测集(第 8 课):给系统"标准答案",量化它到底行不行。

为什么需要 golden 集(讲得出 why):
- 自己感觉"效果不错"不作数,要用一套固定的、有标准答案的考题反复测;
- 改一个 prompt 前后跑同一套 → 数字变了没、变好变坏,一目了然;
- 用**合成**的 JD/简历(红线:真实个人资料绝不进测试与仓库)。

三组评测(见 metrics.py / run.py):
    A 检索 recall@k   词典 vs 向量 vs 混合,谁召回高
    B AI 出题覆盖     计划里有没有覆盖 JD 的高危主题
    C 评分严格度     弱答案分数应显著低于强答案(不许当老好人)
"""
# 合成 JD:点名几类能力,方便断言"该覆盖的主题"
GOLDEN_JD = """
AI 应用研发工程师(校招)
岗位职责:负责基于大模型(LLM/Agent)的 RAG 问答与多轮对话应用开发;
负责知识库问答的召回-精排链路优化。
任职要求:Python 功底扎实,熟悉 pytest;熟悉向量库(chromadb/faiss)与 embedding;
了解 Function Calling、上下文管理;加分项:SQLite、Streamlit、Eval 评测。
"""

# 合成简历:技术面窄,故意漏掉 JD 的 RAG/向量库,并埋没数字的夸大句
GOLDEN_RESUME = """
专业技能:熟练 Python,熟悉 pytest。
项目经历:负责过一个内部小工具的开发,显著提升了团队效率。
这个项目让我精通了大模型 API 调用的各种细节。
教育经历:某某大学 计算机相关专业。
"""

# JD 最该被 AI 出题器抓住的高危主题(大小写不敏感做包含匹配)
EXPECTED_THEMES = ["rag", "向量", "embedding", "多轮对话", "证据", "pytest"]


# 一对强/弱回答,用来测评卷人严不严格(离线脚本按特征给分;live 真给分)
STRONG_ANSWER = (
    "我做了一个 RAG 问答:用 chromadb 存了 2 万条文档切片,检索分两步,先按向量召回 top50,"
    "再用 rerank 精排到 top5,准确率从 61% 提到 83%。踩过的坑是长文档切块重叠率没调好会丢上下文,"
    "后来用 15% 重叠加标题前缀修好了。"
)
WEAK_ANSWER = (
    "我精通 RAG,负责过相关项目,提升了效果,保证了质量,得到了大家一致好评。"
)
